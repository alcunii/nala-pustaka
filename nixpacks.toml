[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd backend && npm ci"]

[phases.build]
cmds = ["echo No build step needed for backend"]

[start]
cmd = "cd backend && node src/server.js"
